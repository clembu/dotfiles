- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/i3/conf:
        create: true
        path: i3/config
    ~/.Xresources:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
